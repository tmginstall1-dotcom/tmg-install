node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Android keystore files (contains private signing keys — never commit)
*.keystore
*.keystore.b64
!android/app/tmg-staff-release.keystore.b64
# The release keystore for CI builds is committed intentionally (internal-use app)
!android/app/tmg-staff-release.keystore
