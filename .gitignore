node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Android keystore files (contains private signing keys — never commit)
*.keystore
*.keystore.b64
# Firebase service account credentials — store in FIREBASE_SERVICE_ACCOUNT env var
attached_assets/*firebase-adminsdk*.json
