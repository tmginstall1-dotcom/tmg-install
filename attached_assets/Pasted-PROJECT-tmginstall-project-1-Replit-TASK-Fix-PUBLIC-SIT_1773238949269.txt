PROJECT: tmginstall project 1 (Replit)

TASK: Fix PUBLIC SITE typography to match Yeezy “T-Star” style + correct sizing. Current headings are the wrong font + wrong scale.
PUBLIC SITE ONLY:
- Landing page + Estimate flow pages (public quote wizard)
- Do NOT touch admin/staff pages (unless they inherit the same global public layout automatically)

STRICT SCOPE:
- Typography ONLY: fonts, font sizes, letter spacing, line-height, text-transform.
- NO changes to routing, components behavior, Supabase, Stripe, Edge Functions, quote logic, maps/catalog logic.
- NO redesign/layout changes besides typography spacing needed to look correct.
- FULL-FILE REPLACEMENTS for every edited file.

FONT REQUIREMENTS (MUST):
1) Use “T-Star” style heading font.
   - Implement a local font called: "TStar"
   - Add font files to: /public/fonts/
     - TStar-Regular.woff2
     - TStar-Bold.woff2
   - Add @font-face rules in global CSS so the site uses these.
   - IMPORTANT: If these font files are missing, DO NOT break the site.
     Fallback order must be:
     "TStar" -> "Teko" -> "Oswald" -> "Arial Narrow" -> sans-serif

2) Body font stays clean:
   - Use "Inter" for body + UI controls.
   - Load Inter via Google Fonts OR existing setup.

TYPOGRAPHY RULES (MUST APPLY GLOBALLY):
A) Global font variables:
   --font-heading: "TStar", "Teko", "Oswald", "Arial Narrow", sans-serif;
   --font-body: "Inter", system-ui, -apple-system, sans-serif;

B) Apply fonts:
   body, button, input, select, textarea, nav, labels -> var(--font-body)
   h1,h2,h3, .hero-title, .display-title, .section-title, .brand-title -> var(--font-heading)

C) IMPORTANT: Stop forced ALL CAPS everywhere.
   - Only the hero H1 can be uppercase if already designed that way.
   - Other headings should NOT be forced uppercase globally.
   - Remove any global `uppercase` that is applied to all headings.

SIZING (THIS IS THE MAIN FIX):
Use clamp() so it looks correct on both mobile + desktop.

1) Hero H1 (landing page main headline):
   - font-size: clamp(44px, 5.2vw, 68px)
   - line-height: 0.95
   - letter-spacing: -0.02em
   - font-weight: 700 (bold)

2) Section headings (H2):
   - font-size: clamp(28px, 3vw, 40px)
   - line-height: 1.05
   - letter-spacing: -0.01em
   - font-weight: 700

3) Subhead / paragraph under hero:
   - font-size: 16px mobile, 18px desktop
   - line-height: 1.55
   - font-weight: 400–500

4) Buttons:
   - font-size: 14px mobile, 15px desktop
   - font-weight: 600
   - DO NOT use the heading font for buttons.

5) Form labels:
   - 12–13px
   - font-weight: 600

6) Step indicator text (“Step 1 of 4”, “Step 2 of 4”):
   - 12–13px
   - keep it subtle

IMPLEMENTATION DETAILS (DO THIS EXACTLY):
1) Locate the public global stylesheet (e.g. src/index.css, src/styles/globals.css, or similar).
   - Add the @font-face for TStar (Regular + Bold) referencing:
     /fonts/TStar-Regular.woff2
     /fonts/TStar-Bold.woff2
   - Add CSS variables + global rules for headings/body + sizing.
   - Ensure Inter is loaded (via existing or Google Fonts).

2) If Tailwind exists:
   - Update tailwind.config to add:
     fontFamily: { heading: ['TStar','Teko','Oswald','Arial Narrow','sans-serif'], body: ['Inter','system-ui','sans-serif'] }
   - Remove any global typography plugin config that forces uppercase or oversized headings.
   - Update the hero headline class to use `font-heading` and the clamp sizing rules (either via CSS class or Tailwind custom class).

3) IMPORTANT: DO NOT alter layout.
   - Only adjust margins/padding if the new font causes clipping.
   - Keep monochrome styling.

DELIVERABLE:
- Full-file replacements for each edited file.
- Confirm the exact files changed.
- After change: landing H1 should look like Yeezy T-Star style (tight, bold, condensed), but NOT giant.